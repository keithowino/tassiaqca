export const OFFERING_CALENDAR_TYPE = Object.freeze({
	INTERNAL: "INTERNAL",
	EXTERNAL: "EXTERNAL",
});

export const OFFERING_CALENDAR_TYPE_VALUES = Object.freeze(
	Object.values(OFFERING_CALENDAR_TYPE),
);
