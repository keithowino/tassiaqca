export const OFFERING_DURATION_UNITS = Object.freeze({
	MINUTES: "MINUTES",
	HOURS: "HOURS",
	DAYS: "DAYS",
	WEEKS: "WEEKS",
	MONTHS: "MONTHS",
});

export const OFFERING_DURATION_UNIT_VALUES = Object.freeze(
	Object.values(OFFERING_DURATION_UNITS),
);

export const OFFERING_DURATION_STATUS = Object.freeze({
	ACTIVE: "ACTIVE",
	ARCHIVED: "ARCHIVED",
});

export const OFFERING_DURATION_STATUS_VALUES = Object.freeze(
	Object.values(OFFERING_DURATION_STATUS),
);
