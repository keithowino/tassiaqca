- Considering the `marketplaceOfferingsQuerySchema`, We are intentionally limiting the first query contract to filters the Offering source can actually support.
- No premature:

    ```text
    category
    location
    distance
    price
    rating
    availability
    business
    neighborhood
    ```

    Those come later when their owning infrastructure exists.
